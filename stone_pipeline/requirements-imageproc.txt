# Optional image-processing extras for the scraped-photo enhancement pipeline
# (stone_pipeline/io/image_processing.py). The FAITHFUL enhancement + downscale
# chain needs only OpenCV/numpy/Pillow (already core deps); these are ONLY required
# for de-watermarking flagged sources (varsha): the logo is located by its hue and
# inpainted with LaMa. Install on the AWS image-processing container/job; the core
# pipeline does not need them.
#
#   pip install -r stone_pipeline/requirements-imageproc.txt
#
# NOTE: Florence-2 (transformers/einops/timm/flash_attn) was dropped — open-vocab
# detection was unreliable on the faint centred logo. Colour-locate + LaMa only.
# GPU is optional but a large speed-up for a big batch; CPU works fine.

pillow>=10              # keep the modern cp312 wheel; stops pip backtracking to a
                        # pre-3.12 Pillow sdist (which has no wheel and won't compile)
torch>=2.2
torchvision>=0.17
simple-lama-inpainting  # LaMa inpainting wrapper (downloads weights on first use)
spandrel                # loads Real-ESRGAN (RRDBNet) weights for the enhancement engine
