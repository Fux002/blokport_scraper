# Core (required)
polars>=1.0
pyarrow>=15
rapidfuzz>=3.0
pydantic>=2.0
jellyfish>=1.0
pyyaml>=6.0
httpx>=0.27
curl_cffi>=0.7          # Cloudflare-fronted SlabWare scrapers (chrome TLS impersonation)
boto3>=1.34             # S3 image staging backend (images.mode = s3)

# Image stage — faithful enhancement + Lanczos upscale (CPU, always available).
opencv-python-headless>=4.9
numpy>=1.26
pillow>=10.0

# De-watermarking is FAL FLUX Fill -- a HOSTED API call (no torch, no GPU), so it belongs in :core: a
# source the page marks watermarked=off/enhance=off finishes entirely on :core (resize + de-watermark),
# and the GPU (torch/ESRGAN, requirements-imageproc.txt) is reserved for enhance=on. fal-client + requests
# are the only libs the de-watermarker needs; they are lightweight. FAL runs only when a source is
# watermarked=on AND enhance=off (else the GPU de-watermarks + upscales in one pass).
fal-client>=0.4
requests>=2.31

# Optional (lazy-imported, config-gated)
browser_cookie3>=0.19   # only if a scraper needs browser cookie auth (none do today)
# sentence-transformers  # variation tier 8 (matching.enable_semantic)
# fal-client>=0.4        # variant texture generation (image_pipeline/genetate_images.py)

# Dev
pytest>=8.0
